/**
 * Public Drizzle schema entrypoint for applications that install this component.
 * Tables are added here as top-level named exports so the consuming application's
 * Drizzle Kit entrypoint can re-export and discover them.
 */
export const modelSettingsSchemaOwner =
  "@mia-cx/protocord-model-settings" as const;
