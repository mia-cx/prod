/**
 * Public Drizzle schema entrypoint for applications that install this extension.
 * Tables are added here as top-level named exports so the consuming application's
 * Drizzle Kit entrypoint can re-export and discover them.
 */
export const permissionsSchemaOwner = "@protocord/permissions" as const;
